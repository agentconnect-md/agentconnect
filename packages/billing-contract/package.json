{
  "name": "@agentconnect.md/billing-contract",
  "version": "0.1.0-dev",
  "description": "AgentConnect billing wire contract — zod schemas and types, no implementation",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "main": "dist/index.js",
  "module": "dist/index.js",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "prepack": "pnpm run build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "sideEffects": false,
  "types": "dist/index.d.ts",
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "json": "^11.0.0",
    "typescript": "^6.0.3"
  },
  "keywords": [
    "agentconnect",
    "billing",
    "contract",
    "zod"
  ],
  "engines": {
    "node": ">=24.12.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
