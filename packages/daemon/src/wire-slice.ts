// The one-frame slicing helpers are protocol's since the memory op set became a wire contract; re-served for local callers.
export { REPLY_BUDGET, encodedBytes, fitToBudget, utf8Boundary } from '@agentconnect.md/protocol'
