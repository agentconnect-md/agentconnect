/** The named refusal a turn-time Gitea host disagreement takes (gitea-integration.md §3, §11);
 *  the fence itself is the Gitea turn-final member that reports it. */
export const GITEA_HOST_MISMATCH_REASON = 'gitea_host_mismatch'
